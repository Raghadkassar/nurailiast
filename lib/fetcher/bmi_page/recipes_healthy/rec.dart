import 'package:flutter/material.dart';

class rec_healthy extends StatelessWidget {
  const rec_healthy({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
