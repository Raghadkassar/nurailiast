import 'package:flutter/material.dart';

class cons_wheigt extends StatelessWidget {
  const cons_wheigt({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
